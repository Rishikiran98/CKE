# CKE
Convergent Knowledge Engine (CKE) is a modular AI architecture that converts unstructured information into a contextual knowledge graph and uses sparse graph-based retrieval and reasoning to answer queries efficiently with minimal context.
